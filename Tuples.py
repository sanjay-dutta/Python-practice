# Tuples
numbers = (1, 2, 3)
print(numbers[0])

# Tuple object does not support item assignment
numbers = (1, 2, 3)
numbers[0] = 10
print(numbers)

#unpacking 
coordinates = (1, 2, 3)
x, y, z = coordinates
print(x)